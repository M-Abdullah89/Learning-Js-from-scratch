const arr11 = [1, 2, 3, 4, 5, "Cat", "Dog", "Lion"];
// ? converts an array to a string
// ? does not change the original array.
// ? Any separator can be specified. The default is comma (,).
console.log(arr11.join(" "));