const arr4 = [1, 2, 3, 4, 5, "Cat", "Dog", "Lion"];
// ? removes the last element from the array
// ? //? changes original array
// ? returns the last element of an array
// example
console.log(".pop");
console.log(arr4.pop()); //* removes and returns Lion
console.log(arr4); //* lion is removed