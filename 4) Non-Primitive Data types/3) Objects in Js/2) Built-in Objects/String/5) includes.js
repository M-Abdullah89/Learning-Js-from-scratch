//! returns true if a string contains a specified string. Otherwise it returns false.
//? it is case sensitive

let text = "Abdullah";
let result = text.includes("h",5);
//? 0 is default start point
 console.log(result)//* true
