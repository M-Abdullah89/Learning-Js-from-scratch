//! method adds & updates an element to a map.

const person = new Map();

person.set("name","Abdullah");
person.set("class",10);
person.set("age",15);

console.log(person)
console.log(person.get("age"))