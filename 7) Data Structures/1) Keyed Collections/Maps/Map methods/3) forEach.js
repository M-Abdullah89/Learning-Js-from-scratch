//! invokes a function for each map element:

let person = new Map([
    ['name', "Abdullah"],   
    ['class', 10],
    ['age', 15]
]);

fruits.forEach((value, key) => {
    console.log(key, value);
});

